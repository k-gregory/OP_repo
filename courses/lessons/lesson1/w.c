#include <stdio.h>

int main(void) {
  printf("char is %i\n", sizeof(char));
  printf("int is %i\n", sizeof(int));
  printf("pointer is %i\n", sizeof(void *));
  printf("double is %i\n", sizeof(double));
  return 0;
}
